// 1. Create a variable called 'multiply' that multiplies num1 and num2 to get 62.
let num1 = 2;
let num2 = 31;
let multiply = num1 * num2; // 2 * 31 = 62

// 2. Create a variable called 'random' that generates a random integer greater than 0.
let random = Math.floor(Math.random() * 100) + 1; // Random integer between 1 and 100

// 3. Create a variable called 'mod' that calculates the remainder of num3 divided by num4.
let num3 = 11;
let num4 = 7;
let mod = num3 % num4; // 11 % 7 = 4

// 4. Create a variable called 'max' that finds the highest number in a set.
let max = Math.max(10, 20, 15); // The highest number is 20

// Export the variables if needed for testing
